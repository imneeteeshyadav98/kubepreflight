kubepreflight scan --target-version 1.33 --upgrade-context audit-only --output all --output-dir . --serve-report never --terminal-output full  # profile=Profile C - webhook backend evidence denied (Services+EndpointSlices), KUBECONFIG=<redacted-temp-path>
